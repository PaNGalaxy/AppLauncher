trame: 
  cmd:
    - python
    - -m
    - launcher_app.app
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server
    - --session
    - ${id}
    - timeout=0